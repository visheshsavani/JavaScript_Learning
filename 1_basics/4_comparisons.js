/*
console.log(null > 0);
console.log(null == 0);
console.log(null >= 0);
*/

// in the above eg we can see that the output is different when we >= operator because
// in javascript >= and > operators works differently

console.log("2" === 0);
// in the above eg we can see === this operator is used to for strict checking i.e before operation 
// it checks whether two variables has same datatype or not if not same datatype then output is false